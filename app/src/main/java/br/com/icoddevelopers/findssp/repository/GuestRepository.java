/*
package br.com.icoddevelopers.findssp.repository;

import android.content.Context;

public class GuestRepository {

    private static GuestRepository INSTANCE;
    private GuestDataBase mGuestDataBase;


    private GuestRepository(Context context) {
        this.mGuestDataBase = new GuestDataBase(context);
    }

    public static synchronized GuestRepository getInstance(Context context) {
        if (INSTANCE == null) {
            INSTANCE = new GuestRepository(context);
        }
        return INSTANCE;
    }

}
*/